import React from "react";
import styled from "styled-components";

const ProfessorWrite = (props) => {
  return <Wrapper>ProfessorWrite</Wrapper>;
};

const Wrapper = styled.div``;

export default ProfessorWrite;
