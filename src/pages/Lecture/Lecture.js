import React from "react";
import styled from "styled-components";
import PageTitle from "../../components/Titles/PageTitle";

const Lecture = (props) => {
  return (
    <Wrapper>
      <PageTitle title={"Lecture"} />
      Lecture
    </Wrapper>
  );
};

const Wrapper = styled.div``;

export default Lecture;
